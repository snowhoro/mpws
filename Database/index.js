/*if(!global.hasOwnProperty('db')){
	var mongoose = require('mongoose');
	var dbName   = 'webservice';
	mongoose.connect('mongodb://imagecampus:image123@ds033113.mongolab.com:33113/' + dbName);
	
	global.db = {
		mongoose : mongoose,
		User: require('./User')	(mongoose)
	};
}

module.exports = global.db;*/